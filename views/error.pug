extends layout.pug 

block content 
    h1 Error Found: (#{err.status})
    h2= err.message
    h2 Stack Trace:
    p= err.stack
    h2 Click Below to go back to the main page
    nav.nav
        a(href='/')
            span &larr;
            |  Back